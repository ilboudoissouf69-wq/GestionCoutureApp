<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="GestionCoutureApp.Views.MaterielsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BackgroundBrush}">

    <Grid Margin="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre + boutons -->
        <Grid Grid.Row="0"
                Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                    VerticalAlignment="Center">
                <TextBlock Text="Materiaux Supplementaires"
                           FontSize="22"
                        FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"/>
                <Border Height="3"
                        Width="48"
                        CornerRadius="2"
                        Background="#CC0000"
                        HorizontalAlignment="Left"
                        Margin="0,6,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Button x:Name="BtnAjouter"
                        Content="+ Ajouter"
                        Height="38"
                        Padding="16,0"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Background="{StaticResource AccentBrush}"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnAjouter_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="BtnModifier"
                        Content="Modifier"
                        Style="{StaticResource BtnGhost}"
                        Click="BtnModifier_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="BtnSupprimer"
                        Content="Supprimer"
                        Style="{StaticResource BtnGhost}"
                        Foreground="{StaticResource DangerBrush}"
                        Click="BtnSupprimer_Click"/>
            </StackPanel>
        </Grid>

        <!-- Filtre par commande -->
        <Grid Grid.Row="1"
                Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0"
                      x:Name="CmbCommande"
                      Height="38"
                    FontSize="13"
                      SelectionChanged="CmbCommande_SelectionChanged"/>

            <TextBlock Grid.Column="1"
                       x:Name="TxtTotal"
                       FontSize="16"
                    FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"/>
        </Grid>

        <!-- Recherche -->
        <TextBox Grid.Row="2"
                 x:Name="TxtRecherche"
                 Style="{StaticResource ModernTextBox}"
                 Margin="0,0,0,14"
                 TextChanged="TxtRecherche_TextChanged"/>

        <!-- Tableau -->
        <Border Grid.Row="3"
                Style="{StaticResource Card}"
                Padding="0">
            <DataGrid x:Name="GridMateriels"
                      Style="{StaticResource ModernGrid}"
                      AutoGenerateColumns="False"
                      SelectionMode="Single"
                    IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Commande"
                                        Binding="{Binding IdCommande, StringFormat='CMD-{0}'}"
                                        Width="85"/>
                    <DataGridTextColumn Header="Client"
                                        Binding="{Binding Commande.Client.Nom}"
                                        Width="*"/>
                    <DataGridTextColumn Header="Piece"
                                        Binding="{Binding PieceCommande.TypeVetement}"
                                        Width="120"/>
                    <DataGridTextColumn Header="Designation"
                                        Binding="{Binding Designation}"
                                        Width="*"/>
                    <DataGridTextColumn Header="Qte"
                                        Binding="{Binding Quantite}"
                                        Width="55"/>
                    <DataGridTextColumn Header="Prix unit."
                                        Binding="{Binding PrixUnitaireAffiche}"
                                        Width="120"/>
                    <DataGridTextColumn Header="Montant"
                                        Binding="{Binding MontantAffiche}"
                                        Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>